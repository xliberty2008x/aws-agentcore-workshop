
| **Test Case ID** | **Priority** | **Summary**                                       | **Category**                         | **Description**                                                                                                         | **Preconditions**                                                                                                | **Steps to Reproduce**                                                                                                                                                  | **Expected Result**                                                                                                                                                                                                                 |
| ---------------- | ------------ | ------------------------------------------------- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AUTH-001         | High         | Valid JWT allows agent invocation                 | Inbound Authentication (Cognito JWT) | A user with a valid Cognito JWT access token can invoke the agent successfully.                                         | Cognito user exists; valid JWT access token; agent endpoint URL reachable.                                       | 1. Call the AgentCore Gateway/Agent endpoint with header `Authorization: Bearer <valid_access_token>`. 2. Send a prompt that triggers normal agent flow.                | Request succeeds (HTTP `200`), response contains agent output and tool/OAuth orchestration results as applicable.                                                                                                                   |
| AUTH-002         | High         | Malformed JWT rejected                            | Inbound Authentication (Cognito JWT) | A malformed JWT access token is rejected.                                                                               | Test access to endpoint; ability to craft malformed JWT.                                                         | 1. Send request with `Authorization: Bearer <malformed_token>`.                                                                                                         | Request rejected with `401 Unauthorized` (or `403 Forbidden` if framework uses authorization semantics). No tool execution.                                                                                                         |
| AUTH-003         | High         | Expired JWT rejected                              | Inbound Authentication (Cognito JWT) | An expired JWT access token is rejected.                                                                                | Valid user; expired JWT created (past `exp`).                                                                    | 1. Send request using expired JWT.                                                                                                                                      | Rejected with `401 Unauthorized` (or `403 Forbidden`), no downstream calls to Gateway/Runtime.                                                                                                                                      |
| AUTH-004         | High         | Missing Authorization header                      | Inbound Authentication (Cognito JWT) | Request without JWT is rejected.                                                                                        | Endpoint reachable.                                                                                              | 1. Call endpoint without `Authorization` header.                                                                                                                        | Rejected with `401 Unauthorized` and no agent execution.                                                                                                                                                                            |
| AUTH-005         | High         | Wrong issuer/audience rejected                    | Inbound Authentication (Cognito JWT) | JWT signed by wrong issuer/audience should be rejected.                                                                 | Ability to use JWT from different Cognito app/tenant or wrong audience claim.                                    | 1. Send request with JWT where `aud`/`iss` does not match expected.                                                                                                     | Rejected with `401 Unauthorized` or `403 Forbidden`.                                                                                                                                                                                |
| AUTH-006         | Medium       | JWT missing required claims                       | Inbound Authentication (Cognito JWT) | JWT lacking required claims (e.g., subject/user id claim expected by app) is rejected or results in safe denial.        | Ability to remove/alter claims without breaking signature (or use an invalid token that fails claim validation). | 1. Send request with token that fails claim validation.                                                                                                                 | Rejected with `401/403` and no agent/tool invocation; error message does not leak secrets.                                                                                                                                          |
| AUTH-007         | Medium       | Valid JWT but unauthorized user denied            | Inbound Authentication (Cognito JWT) | Valid token for a user that lacks required role/scopes is denied.                                                       | RBAC/scopes enabled (or configured authorization logic); test users with/without access.                         | 1. Call endpoint with valid JWT for unauthorized user.                                                                                                                  | Rejected with `403 Forbidden` (or equivalent).                                                                                                                                                                                      |
| AUTH-008         | Low          | Token with `alg=none` rejected                    | Inbound Authentication (Cognito JWT) | Ensure “algorithm confusion”/`none`-alg tokens are not accepted.                                                        | Ability to craft such a token.                                                                                   | 1. Send request with JWT signed with `alg=none` (or unsupported alg).                                                                                                   | Rejected (`401/403`).                                                                                                                                                                                                               |
| GATE-001         | High         | Tool identified and routed (positive)             | Gateway & Tool Calling               | Agent decides it needs `get_workshop_doc` and Gateway routes correctly to Google Docs tool/handler.                     | Gateway configured with tool `get_workshop_doc`; Google Docs API credentials set; test doc exists.               | 1. Auth as valid user. 2. Send prompt like “What does the workshop say about X?” referencing workshop doc intent. 3. Observe Gateway routing logs/trace (if available). | Gateway returns `200`; agent output includes content derived from the Google Doc; tool call is executed exactly once (or expected count).                                                                                           |
| GATE-002         | High         | Tool not configured in Gateway                    | Gateway & Tool Calling               | Agent attempts to call a tool not present in Gateway target configuration.                                              | Gateway does NOT expose tool (e.g., remove/disable mapping for a specific tool name).                            | 1. Trigger agent to request a non-existent tool (e.g., prompt that causes wrong tool selection).                                                                        | Agent/Gateway fails safely: request returns controlled error (e.g., `400/500` depending on API design) and agent responds with a helpful fallback; no crash.                                                                        |
| GATE-003         | High         | Google Docs returns document not found            | Gateway & Tool Calling               | Tool handler calls Google Docs with incorrect `documentId` and receives “document not found”; agent handles gracefully. | Ability to supply/force invalid `documentId` via prompt or test configuration.                                   | 1. Trigger tool call using wrong `documentId` (or configure tool to point to non-existent doc). 2. Invoke agent.                                                        | Agent responds with a helpful message indicating it couldn’t access the document; HTTP `200` from agent endpoint is acceptable if design chooses “soft-fail”, or controlled error if design uses hard fail. No stack trace to user. |
| GATE-004         | High         | Gateway timeout on slow Google Docs call          | Gateway & Tool Calling               | Google Docs API call exceeds Gateway timeout; Gateway times out and agent reports timeout gracefully.                   | Gateway timeout set (e.g., 10–30s); ability to simulate slowness (network throttle/mocking).                     | 1. Simulate Google Docs handler delay > Gateway timeout. 2. Invoke agent that triggers tool call.                                                                       | Gateway times out with a controlled response (e.g., `504` or agent-friendly message). Agent does not crash; user sees timeout/failure guidance.                                                                                     |
| GATE-005         | Medium       | Google Docs API returns 401/403                   | Gateway & Tool Calling               | OAuth token used by server-to-Google is invalid/expired or lacks permissions; API returns auth/permission error.        | Server OAuth credentials misconfigured or test scope removed.                                                    | 1. Trigger tool call. 2. Observe Google Docs API response.                                                                                                              | Agent returns helpful “couldn’t access document” message; no internal credentials leak.                                                                                                                                             |
| GATE-006         | Medium       | Tool response schema mismatch                     | Gateway & Tool Calling               | Tool returns malformed/partial payload; Gateway/Runtime handles validation failures.                                    | Ability to mock tool response to invalid schema.                                                                 | 1. Mock tool handler to return unexpected JSON shape. 2. Trigger tool call.                                                                                             | Runtime validates and fails safely: helpful fallback message; consistent error handling path.                                                                                                                                       |
| GATE-007         | Medium       | Correct argument mapping (documentId)             | Gateway & Tool Calling               | Ensure tool arguments (e.g., `documentId`) are passed correctly from agent to Gateway to tool handler.                  | Known doc with unique content; tool argument propagation is observable in logs/trace.                            | 1. Trigger tool call. 2. Verify `documentId` used in the Google Docs request (logs/tracing).                                                                            | Google Docs request uses expected `documentId`, and resulting agent answer corresponds to that document’s content.                                                                                                                  |
| GATE-008         | Medium       | Multiple tool calls in one request                | Gateway & Tool Calling               | If the agent performs multiple calls (e.g., fetch metadata then fetch content), Gateway routes each properly.           | Agent prompts can cause multi-step retrieval; tool supports multiple operations.                                 | 1. Send a prompt that requires more than one tool call. 2. Track number of calls via logs/tracing.                                                                      | All tool calls succeed or fail safely; final answer matches combined results; no partial/mixed content.                                                                                                                             |
| GATE-009         | Medium       | Concurrent requests route to correct target       | Gateway & Tool Calling               | Ensure concurrency doesn’t mix tool routing/results between users or requests.                                          | Two test users or two distinct prompts with distinct document content.                                           | 1. Send concurrent requests from user A and B. 2. Confirm each request’s tool calls use correct session/user context.                                                   | Each user receives content from the correct document/session; no cross-talk.                                                                                                                                                        |
| GATE-010         | Low          | Rate limiting from Google Docs (429)              | Gateway & Tool Calling               | Google Docs returns 429; agent handles with retry policy or graceful failure.                                           | Ability to simulate 429 (throttle/mocking).                                                                      | 1. Trigger tool call repeatedly to hit rate limit.                                                                                                                      | Controlled behavior: either retry with backoff (if implemented) or a helpful failure message; no crash.                                                                                                                             |
| OAUTH-001        | High         | First-time consent returns authorization_url      | Outbound OAuth (Google Docs Consent) | First tool call returns `authorization_url` for user consent instead of immediately executing.                          | User has no stored/valid consent token; tool configured to request consent.                                      | 1. Auth as user with no consent. 2. Send prompt that triggers `get_workshop_doc`.                                                                                       | Response includes `authorization_url` (and any required consent metadata like state). Agent endpoint returns guidance to user. Tool execution does not proceed to fetch content until consent.                                      |
| OAUTH-002        | High         | authorization_url contains correct params         | Outbound OAuth (Google Docs Consent) | The returned `authorization_url` includes correct scopes, redirect URI, and state binding to the user/session.          | Consent flow instrumentation available (or capture URL parameters).                                              | 1. Trigger first-time scenario. 2. Inspect `authorization_url` query params.                                                                                            | `authorization_url` includes expected `client_id`, `redirect_uri`, `scope`, and a state value tied to the current user/session; no sensitive data exposed.                                                                          |
| OAUTH-003        | High         | Post-consent second call returns document content | Outbound OAuth (Google Docs Consent) | After user grants consent, the second tool call executes and returns document content.                                  | User completes consent in Google OAuth UI for this environment; token exchange succeeds.                         | 1. Trigger first-time call to obtain `authorization_url`. 2. Complete consent. 3. Invoke agent/tool again with same user.                                               | Second call returns document content (HTTP `200`); no new authorization_url prompt.                                                                                                                                                 |
| OAUTH-004        | High         | Existing consent reuses token (no re-prompt)      | Outbound OAuth (Google Docs Consent) | A user with still-valid consent token executes tool immediately without asking consent again.                           | User has valid stored consent/refresh token.                                                                     | 1. Invoke agent/tool again after consent within token validity window.                                                                                                  | Tool executes immediately; no `authorization_url` returned; content returned successfully.                                                                                                                                          |
| OAUTH-005        | Medium       | Consent token expired triggers refresh/re-consent | Outbound OAuth (Google Docs Consent) | When stored consent/refresh token is expired/invalid, flow requests consent again or refreshes properly.                | Ability to force token expiration/invalidation.                                                                  | 1. Invalidate stored refresh token. 2. Trigger tool call.                                                                                                               | System either refreshes automatically (if possible) or returns `authorization_url` again; user receives clear next steps; no crash.                                                                                                 |
| OAUTH-006        | Medium       | Consent revoked by user                           | Outbound OAuth (Google Docs Consent) | If user revoked Google access, tool should handle “invalid_grant”/permission errors gracefully.                         | After consent, revoke access in Google account security settings (or mock invalid_grant).                        | 1. Trigger tool call after revocation.                                                                                                                                  | Tool returns consent URL again or produces helpful failure guiding user to re-consent; no stack trace.                                                                                                                              |
| OAUTH-007        | Medium       | OAuth callback/state mismatch                     | Outbound OAuth (Google Docs Consent) | Incorrect or tampered OAuth `state` is rejected.                                                                        | Ability to modify state parameter or replay callback with wrong state.                                           | 1. Complete consent using a state tied to a different user/session (or tamper state). 2. Observe callback handling.                                                     | Callback handler rejects mismatch; user receives error; no token stored for wrong session.                                                                                                                                          |
| OAUTH-008        | High         | Consent isolation between users                   | Outbound OAuth (Google Docs Consent) | User A’s consent must not be reused for user B.                                                                         | Two users, only user A has consent.                                                                              | 1. User A completes consent. 2. User B calls the tool.                                                                                                                  | User B receives `authorization_url` (or equivalent consent requirement) and does not receive user A’s document access.                                                                                                              |
| OAUTH-009        | Medium       | Token exchange failure handled                    | Outbound OAuth (Google Docs Consent) | If exchange of authorization code for tokens fails (network/invalid code), system handles gracefully.                   | Ability to mock token exchange failure.                                                                          | 1. Trigger OAuth callback with failing exchange scenario. 2. Return to agent/tool call.                                                                                 | Flow returns helpful error/next steps (e.g., re-initiate consent). No crash; no indefinite hanging.                                                                                                                                 |
| OAUTH-010        | Low          | Token persistence across service restart          | Outbound OAuth (Google Docs Consent) | Stored consent token remains usable after runtime restarts.                                                             | Ability to restart AgentCore Runtime/Gateway; persistent storage configured.                                     | 1. Complete consent for user. 2. Restart service. 3. Trigger tool call again.                                                                                           | Token is still valid for immediate execution; no unnecessary consent prompt.                                                                                                                                                        |
| RUNTIME-001      | High         | Agent uses `get_workshop_doc` when needed         | AgentCore Runtime & Logic            | For a workshop-specific question, the agent decides to call the Google Docs tool and uses results to answer.            | Agent configured with tool; doc contains relevant text for question.                                             | 1. Prompt: ask about a fact contained in workshop doc. 2. Observe whether tool is invoked.                                                                              | Agent invokes tool and returns answer grounded in retrieved document content.                                                                                                                                                       |
| RUNTIME-002      | High         | Agent does not use tool for general chat          | AgentCore Runtime & Logic            | For a general prompt, agent answers directly without calling Google Docs tool.                                          | Tool availability exists but agent should decide “no tool needed”.                                               | 1. Prompt: “hello, how are you?” 2. Monitor tool-call logs.                                                                                                             | Response is direct; no tool invocation; no OAuth/Google calls performed.                                                                                                                                                            |
| RUNTIME-003      | High         | Tool failure triggers helpful fallback            | AgentCore Runtime & Logic            | When tool fails (API error/timeout/invalid token), agent returns a helpful message instead of crashing.                 | Induce failure (wrong `documentId`, mocked 500, or forced timeout).                                              | 1. Trigger tool call that will fail. 2. Send prompt requesting doc info.                                                                                                | Agent replies with a safe, helpful message (e.g., “couldn't access the document… try again later”) and does not crash.                                                                                                              |
| RUNTIME-004      | High         | 5 consecutive requests no crashes, no mixing      | AgentCore Runtime & Logic            | Agent handles 5 consecutive requests from same user without crashing or mixing session context.                         | Known user; consistent session mechanism; doc content stable.                                                    | 1. Send 5 prompts sequentially (including both tool-required and general prompts if possible). 2. Verify each response references correct context.                      | All 5 requests succeed; correct tool usage per prompt; no session data leakage; no cross-request contamination.                                                                                                                     |
| RUNTIME-005      | High         | Concurrent users no cross-session leakage         | AgentCore Runtime & Logic            | With concurrent requests, session data and consent/tool contexts remain isolated per user.                              | Two users; distinct prompts (one requires tool, one general).                                                    | 1. Run concurrent requests for user A and B. 2. Verify tool-call logs and answers align with each user’s intent.                                                        | No leakage: user A doesn’t receive user B’s answers/tool outcomes and vice versa.                                                                                                                                                   |
| RUNTIME-006      | Medium       | Unknown tool request handled safely               | AgentCore Runtime & Logic            | If the agent attempts an unsupported/unknown tool, system fails safely.                                                 | Agent is allowed to “hallucinate” tool calls; Gateway lacks that tool mapping.                                   | 1. Use prompt that causes agent to request an unknown tool.                                                                                                             | Controlled error or fallback message; no runtime crash; logs show safe handling.                                                                                                                                                    |
| RUNTIME-007      | Medium       | Correct behavior after tool-timeout               | AgentCore Runtime & Logic            | After a timeout failure, next request still works normally (no poisoned state).                                         | Induce one timeout via mocking, then restore normal behavior.                                                    | 1. Trigger tool call with forced timeout. 2. Immediately send a new prompt for the same user.                                                                           | Second request recovers: either succeeds (if restored) or returns consistent fallback without crashing/state corruption.                                                                                                            |
| RUNTIME-008      | Medium       | Performance: existing consent response time       | AgentCore Runtime & Logic            | When consent already exists, end-to-end latency stays within acceptable bounds.                                         | User with existing consent; known doc size.                                                                      | 1. Trigger tool-required prompt. 2. Measure p95 latency over multiple runs (e.g., 10).                                                                                  | Responses meet target latency budget (define acceptable threshold for workshop), and remain stable without timeouts.                                                                                                                |
| RUNTIME-009      | Medium       | Performance: first-time consent returns quickly   | AgentCore Runtime & Logic            | First-time flow should return `authorization_url` quickly without waiting for document content.                         | User has no consent yet.                                                                                         | 1. Trigger tool call for first-time consent. 2. Measure time to first response.                                                                                         | Response returns quickly with `authorization_url` (not blocked by document fetch); user can complete consent.                                                                                                                       |
| RUNTIME-010      | Low          | Stability soak (optional)                         | AgentCore Runtime & Logic            | Under a longer run, system remains stable and doesn’t degrade or leak resources.                                        | Test environment; ability to automate repeated requests.                                                         | 1. Run a soak test (e.g., 100–500 requests with mixed prompts). 2. Track memory/CPU and error rate.                                                                     | Low error rate; no crashes; no growing memory leak; consistent behavior.                                                                                                                                                            |


