# Archive

Ця папка містить legacy-артефакти, які не є частиною основного E2E флоу воркшопу.

- `legacy-docs/` — старі чернетки та нотатки, які можуть бути корисні лише як історичний контекст.
- `legacy-modules/` — попередні модулі та допоміжні скрипти.
- `legacy-notebooks/` — чернетки/старі ноутбуки.

Основна робота ведеться у корені репозиторію: `workshop_google_docs_rag_e2e.ipynb` + `runtime_app_agentcore_full.py`.
